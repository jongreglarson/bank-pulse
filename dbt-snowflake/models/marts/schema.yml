version: 2

models:
  - name: gold_bank_metrics
    description: "Gold analytics layer with business metrics, peer groupings, and QoQ changes. Mirrors the bundle's gold_bank_metrics table."
    columns:
      - name: cert
        description: "FDIC certificate number."
        tests:
          - not_null
      - name: report_date
        description: "Quarter-end report date."
        tests:
          - not_null
