version: 2

sources:
  - name: bronze
    database: BANK_PULSE
    schema: BRONZE
    description: "Raw FDIC BankFind data loaded by ingestion/load_snowflake.py. Each row stores one JSON record as a VARIANT."
    tables:
      - name: financials
        description: "Per-institution quarterly financial data from the FDIC financials endpoint."
