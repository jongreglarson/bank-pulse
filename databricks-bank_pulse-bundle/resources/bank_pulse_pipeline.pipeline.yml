resources:
  pipelines:
    bank_pulse_pipeline:
      name: Bank-Pulse DLT Pipeline
      catalog: main
      target: bank_pulse_dlt
      libraries:
        - file:
            path: ${workspace.file_path}/src/01_bronze_ingestion_pipeline.py
        - file:
            path: ${workspace.file_path}/src/02_silver_ingestion_pipeline.py
        - file:
            path: ${workspace.file_path}/src/03_gold_metrics_pipeline.py
      serverless: true
      continuous: false
      channel: CURRENT
      edition: ADVANCED
